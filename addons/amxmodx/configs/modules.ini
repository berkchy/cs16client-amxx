;;;
; AMX Mod X module list (aarch64 / 64-bit-cell build).
;
; The loader resolves <name>_amxx_amd64.so from addons/amxmodx/modules.
; Only the modules used by the bundled plugin set are enabled - the rest are
; shipped but commented out, so nothing extra gets loaded or errors out.
;;;

engine
fakemeta
cstrike

; ---- not used by the bundled set; uncomment to enable ----
;fun
;geoip
;sockets
;regex
;nvault
;csx
;json
;sqlite